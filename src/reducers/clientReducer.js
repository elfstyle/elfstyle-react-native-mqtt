import { } from '../types'

const initialState = {
    ref: null,
    connected: false,
}

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
